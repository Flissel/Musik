[package]
name = "musik-app"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[dependencies]
analysis = { version = "0.1.0", path = "../analysis" }
anyhow = "1.0.104"
audio-core = { version = "0.1.0", path = "../audio-core" }
audio-engine = { version = "0.1.0", path = "../audio-engine" }
eframe = "0.35.0"
egui = "0.35.0"
image = { version = "0.25.10", default-features = false, features = ["png"] }
library = { version = "0.1.0", path = "../library" }
